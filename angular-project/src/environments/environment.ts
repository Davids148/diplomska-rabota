// This file can be replaced during build by using the `fileReplacements` array.
// `ng build ---prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: "AIzaSyBxQ0IriN8453cQpEj70EKphKRv5Yb46kg",
    authDomain: "davidfirebase-7b3fc.firebaseapp.com",
    databaseURL: "https://davidfirebase-7b3fc.firebaseio.com",
    projectId: "davidfirebase-7b3fc",
    storageBucket: "davidfirebase-7b3fc.appspot.com",
    messagingSenderId: "622474662508"
  }
};

/*
 * In development mode, for easier debugging, you can ignore zone related error
 * stack frames such as `zone.run`/`zoneDelegate.invokeTask` by importing the
 * below file. Don't forget to comment it out in production mode
 * because it will have a performance impact when errors are thrown
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
